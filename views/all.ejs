<%- include("header"); -%>

<% var id = currentId %>
<% for(var i = todos.length - 1; i > -1; i--){ %>

    <div class="box item">

        <p class="pAll">
            <% if(i == id) { %>
            <a id="current" href= <%= "/items/" + i %> ><b> <%=  todos[i].periodHeading %></b></a>
            <% } else { %>
                <a href= <%= "/items/" + i %> > <%=  todos[i].periodHeading %></a>
            <% } %>
        </p>
        <input type="hidden" name="item" value=<%= i %>>
        <% if(todos.length > 1) { %>
            <img name="homeImg" id=<%= i %> src="/images/home.png" alt="home">
            <img name="deleteImg" id=<%= i %> src="/images/delete.png" alt="">
        <% } %>
    </div>

<% };  %>
<script src="/js/all.js"></script>

<%- include("footer"); -%>